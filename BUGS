KNOWN BUGS
==========


0.5.0-beta3 (stable, well... almost)
------------------------------------

* Intermitent error happening: when executing a remote command with pssh,
  it sometimes eat some data.

* Lock up with the command run from the console, on some servers only.

* Lock with the monitoring commands (watch, kill, message) in the console

